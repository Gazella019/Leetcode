



class Solution {
public:
    string largestMultipleOfThree(vector<int>& digits){
        
        int i, sum = 0;
        string res;

        for(i=0;i<digits[i].size();i++){
        	sum = sum + digits[i];
        }

        if(sum % 3 == 0)
        	return 
    }

    string make(vector<int>& digits){
    	string s;
    	s = s + digits[i];
    }

};